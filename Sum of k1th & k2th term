class Solution { 
    public static long sumBetweenTwoKth(long A[], long N, long K1, long K2)
    {
        Arrays.sort(A);
        long sum = 0;
        for(long i=K1;i<K2-1;i++)
        {
            sum=sum+A[(int)i];
        }
        return sum;
    }
    
}
